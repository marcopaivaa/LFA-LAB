from node import *

class Edge:
    def __init__(self, fromNode, toNode):
        self.fromNode = fromNode
        self.toNode = toNode

    def removeEdge(edge):
        edges.remove(edge)

    def setName(name):
        self.name = name
    
    def addEdge(to):
        self.edges.append(to)